---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="Services" 
  description="Explore our comprehensive construction services tailored to your specific needs, from industrial buildings to sustainable infrastructure solutions."
>
  <!-- Apply Index Background -->
  <div class="fixed inset-0 -z-10 bg-gradient-to-br from-black/80 via-gray-900 to-black">
    <div class="absolute inset-0 z-0">
      <div class="bg-[radial-gradient(circle_at_30%_30%,rgba(255,107,53,0.1)_0%,transparent_70%)] absolute inset-0"></div>
      <div class="bg-[radial-gradient(circle_at_70%_70%,rgba(255,107,53,0.1)_0%,transparent_70%)] absolute inset-0"></div>
    </div>
  </div>
  
  <!-- Page Header Section (Full Width) -->
  <section class="page-header relative h-[500px] flex items-center justify-center overflow-hidden w-full pb-0">
    <!-- Background Image with Overlay -->
    <div class="absolute inset-0 z-0">
      <img 
        src="/public/projects/naseem.jpg" 
        alt="Services Header Background" 
        class="w-full h-full object-cover"
      />
      <!-- Overlay to ensure text readability -->
      <div class="absolute inset-0 bg-black/60"></div>
    </div>
    
    <!-- Header Content -->
    <div class="header-content text-center max-w-3xl px-5 animate-fade-in-down relative z-10">
      <h1 class="header-title text-5xl md:text-6xl font-bold mb-4 text-white drop-shadow-lg">
        Our <span class="text-primary bg-gradient-to-r from-primary to-accent bg-clip-text">Services</span>
      </h1>
      <p class="header-subtitle text-lg md:text-xl text-gray-300 mb-4">
        Comprehensive construction solutions tailored to your specific needs
      </p>
      <p class="header-description text-base md:text-lg text-gray-300 max-w-2xl mx-auto">
        At Ultraction General Contracting, we offer a comprehensive range of construction services designed to meet the diverse needs of our clients. From industrial, commercial, and residential buildings to specialized infrastructure like bridges, water reservoirs, and heritage revitalization, our experienced team delivers excellence at every stage of the project lifecycle.
      </p>
    </div>
  </section>

  <div class="container mx-auto relative z-10">
    <!-- Services Carousel Section (No Top Padding) -->
    <section class="services-carousel pt-0 pb-20">
      <div class="container mx-auto px-6">
        <!-- Remove the redundant "Our Services" title -->
        <div class="text-center mb-10">
          <p class="text-xl max-w-3xl mx-auto text-gray-300 animate-fade-in">
            Explore our range of specialized construction services
          </p>
        </div>
        
        <div class="services-tabs flex flex-wrap justify-center gap-4 mb-10">
          <button class="service-tab active px-6 py-3 rounded-full text-white font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="buildings">Buildings</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="bridges">Bridges</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="water">Water</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="power">Power</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="seaports">Seaports</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="utilities">Utilities</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="oilgas">Oil & Gas</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="heritage">Heritage</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="pipeline">Pipeline</button>
          <button class="service-tab px-6 py-3 rounded-full text-gray-400 font-medium bg-white/5 border border-white/10 transition-all duration-300 hover:bg-primary/20 hover:text-primary" data-tab="roadworks">Road Works</button>
        </div>
        
        <div class="services-content relative min-h-[500px] perspective-1000">
          <!-- 1. Industrial, Commercial, and Residential Buildings -->
          <div class="service-panel active absolute w-full transition-all duration-700 opacity-100 transform translate-x-0 rotate-y-0" id="buildings-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/bci.jpeg" alt="Buildings Construction" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4 animate-fade-in">Buildings</span>
                <h2 class="text-3xl font-bold mb-6 text-white animate-slide-in-right">Industrial, Commercial, and Residential Buildings</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Specializing in the construction of industrial, commercial, and residential buildings, including palaces, we offer tailored services from planning to execution, ensuring structures meet industry standards, are functional, and reflect aesthetic aspirations.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Industrial Facilities', 'Commercial Spaces', 'Residential Homes', 'Luxury Palaces', 'Quality Execution'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1 animate-bounce-in">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 2. Bridges, Underpasses, and Box Culverts -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="bridges-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/bridge.jpg" alt="Bridges Construction" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Bridges</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Bridges, Underpasses, and Box Culverts</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Providing end-to-end services from planning to construction and handover, we focus on safety, aesthetics, and functionality, delivering structural engineering excellence for resilient transportation and water management systems.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Bridge Construction', 'Box Culverts', 'Safety Standards', 'Aesthetic Integration', 'Structural Resilience'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 3. Water Reservoirs and Pump Stations -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="water-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/WATER RESERVOIR.png" alt="Water Management" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Water</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Water Reservoirs and Pump Stations</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Specializing in construction and maintenance of water reservoirs and pump stations, we ensure efficient water management with an emphasis on sustainability and customized solutions for diverse project requirements.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Pump Station Construction', 'Maintenance Services', 'Efficient Water Flow', 'Sustainable Solutions', 'Custom Engineering'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 4. Power Plants, Distribution Substations, and Control Buildings -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="power-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/POWERPLANT.png" alt="Power Infrastructure" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Power</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Power Plants, Distribution Substations, and Control Buildings</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Providing civil works for power plants, distribution substations, and control buildings, we cover planning to maintenance, adhering to strict safety, functionality, and reliability standards.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Power Plant Civil Works', 'Substation Construction', 'Control Buildings', 'Safety Compliance', 'Reliability Standards', 'Maintenance Services'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 5. Civil Works for Seaports and Airports -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="seaports-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/sea and airports.jpeg" alt="Seaports and Airports" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Seaports</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Civil Works for Seaports and Airports</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Offering comprehensive civil works from planning to construction, we support critical infrastructure in the maritime and aviation sectors, ensuring precision and compliance with international standards.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Seaport Infrastructure', 'Airport Civil Works', 'Planning Services', 'Construction Excellence', 'International Standards', 'Precision Engineering'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 6. Utilities Infrastructure Development -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="utilities-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/Utility.jpeg" alt="Utilities Infrastructure" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Utilities</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Utilities Infrastructure Development</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Covering water distribution, drainage systems, power, and communication lines, we focus on functionality, reliability, and seamless integration into broader networks.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Water Distribution', 'Drainage Systems', 'Power Lines', 'Communication Networks', 'Reliability Focus', 'Network Integration'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 7. Onshore and Offshore Civil Works (Oil & Gas Field) -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="oilgas-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/onshore and offshore.png" alt="Oil & Gas Civil Works" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Oil & Gas</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Onshore and Offshore Civil Works (Oil & Gas)</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Offering planning and execution of essential infrastructure on land and sea, we comply with industry standards, ensuring safety, durability, and environmental responsibility.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Onshore Infrastructure', 'Offshore Construction', 'Planning', 'Safety Compliance', 'Durability Standards', 'Environmental Care'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 8. Heritage Revitalization Projects -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="heritage-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/heritage.png" alt="Heritage Revitalization" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Heritage</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Heritage Revitalization Projects</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Specializing in preserving and restoring historical and cultural landmarks, we use advanced restoration techniques while maintaining historical authenticity and ensuring sustainable integration of modern amenities.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Historical Preservation', 'Restoration Techniques', 'Cultural Landmarks', 'Authenticity Focus', 'Modern Integration', 'Sustainability'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 9. Pipeline and Storm Network -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="pipeline-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/pipeline.jpeg" alt="Pipeline and Storm Network" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Pipeline</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Pipeline and Storm Network</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Providing end-to-end services for pipeline infrastructure and stormwater management, we ensure efficient transport of substances and stormwater flow control, implementing sustainable urban and industrial solutions.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Pipeline Construction', 'Stormwater Management', 'End-to-End Services', 'Efficient Transport', 'Flow Control', 'Sustainable Solutions'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
          
          <!-- 10. Road Works and Earthworks -->
          <div class="service-panel absolute w-full transition-all duration-700 opacity-0 transform translate-x-full rotate-y-15" id="roadworks-panel">
            <div class="service-content flex flex-col lg:flex-row items-center gap-8 lg:gap-16 bg-white/5 backdrop-blur-md rounded-xl p-6 shadow-xl">
              <div class="service-image w-full lg:w-1/2 rounded-xl overflow-hidden shadow-2xl relative group">
                <img src="/public/services/roadworks.jpg" alt="Road Works and Earthworks" class="w-full h-[300px] object-cover transition-transform duration-500 group-hover:scale-105">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              </div>
              <div class="service-details w-full lg:w-1/2">
                <span class="service-category inline-block px-4 py-1 bg-primary/20 text-primary text-sm rounded-full mb-4">Road Works</span>
                <h2 class="text-3xl font-bold mb-6 text-white">Road Works and Earthworks</h2>
                <p class="text-lg leading-relaxed mb-6 text-gray-300">
                  Covering road construction, excavation, and earth-moving projects, we ensure strict compliance with industry regulations, focusing on quality, durability, and sustainability.
                </p>
                <ul class="service-features grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
                  {['Road Construction', 'Excavation Services', 'Earth-Moving Projects', 'Regulatory Compliance', 'Quality Assurance', 'Sustainable Practices'].map((feature, index) => (
                    <li class="flex items-start animate-fade-in" style={`animation-delay: ${index * 100}ms`}>
                      <span class="feature-icon text-primary mr-3">✓</span>
                      <span class="text-gray-300">{feature}</span>
                    </li>
                  ))}
                </ul>
                <a href="/contact" class="inline-block px-8 py-3 bg-gradient-primary text-white font-semibold rounded transition-all duration-300 hover:shadow-lg hover:shadow-primary/30 hover:transform hover:-translate-y-1">Request a Consultation</a>
              </div>
            </div>
          </div>
        </div>

        <!-- Updated Carousel Controls with Extra Spacing -->
        <div class="carousel-controls flex justify-center mt-20 gap-4">
          <button class="carousel-btn bg-transparent border-2 border-primary text-primary w-12 h-12 rounded-full flex items-center justify-center transition-all duration-300 hover:bg-primary hover:text-black text-xl" id="prevBtn">←</button>
          <button class="carousel-btn bg-transparent border-2 border-primary text-primary w-12 h-12 rounded-full flex items-center justify-center transition-all duration-300 hover:bg-primary hover:text-black text-xl" id="nextBtn">→</button>
        </div>
      </div>
    </section>
    
    <!-- Why Choose Section -->
    <section class="why-choose-section py-20">
      <div class="container mx-auto px-6">
        <div class="text-center mb-16 animate-fade-in">
          <h2 class="text-4xl md:text-5xl font-bold mb-6 inline-block relative">
            Why Choose <span class="text-primary">Ultraction</span>
            <span class="absolute bottom-0 left-1/2 w-24 h-1 bg-gradient-primary -translate-x-1/2 rounded-full animate-pulse"></span>
          </h2>
          <p class="text-xl max-w-3xl mx-auto text-gray-300">
            What sets us apart in the construction industry
          </p>
        </div>
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {[
            { icon: 'M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z', title: 'Proven Track Record', text: 'Over 15 years of experience and more than 200 successful projects.' },
            { icon: 'M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z', title: 'Expert Team', text: 'Highly skilled professionals with specialized expertise.' },
            { icon: 'M13 10V3L4 14h7v7l9-11h-7z', title: 'Innovative Approach', text: 'Cutting-edge technologies for modern solutions.' }
          ].map((reason, index) => (
            <div class="reason-card bg-white/5 rounded-xl p-8 backdrop-blur-md border border-white/10 transition-all duration-500 hover:transform hover:-translate-y-2 hover:shadow-lg hover:shadow-primary/20 animate-fade-in" style={`animation-delay: ${index * 200}ms`}>
              <div class="reason-icon w-16 h-16 rounded-full bg-primary/10 flex items-center justify-center mb-6 mx-auto">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d={reason.icon} />
                </svg>
              </div>
              <h3 class="text-2xl font-semibold text-primary mb-4 text-center">{reason.title}</h3>
              <p class="text-gray-300 leading-relaxed text-center">{reason.text}</p>
            </div>
          ))}
        </div>
        <div class="stats mt-16 grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="stat-item text-center animate-fade-in-up" style="animation-delay: 300ms">
            <div class="stat-number text-4xl md:text-5xl font-bold text-primary mb-2" data-count="15">0+</div>
            <div class="stat-label text-sm text-gray-400">Years Experience</div>
          </div>
          <div class="stat-item text-center animate-fade-in-up" style="animation-delay: 400ms">
            <div class="stat-number text-4xl md:text-5xl font-bold text-primary mb-2" data-count="200">0+</div>
            <div class="stat-label text-sm text-gray-400">Projects Completed</div>
          </div>
          <div class="stat-item text-center animate-fade-in-up" style="animation-delay: 500ms">
            <div class="stat-number text-4xl md:text-5xl font-bold text-primary mb-2" data-count="100">0%</div>
            <div class="stat-label text-sm text-gray-400">Client Satisfaction</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Certifications Section -->
    <section class="certifications-section py-20">
      <div class="container mx-auto px-6">
        <div class="text-center mb-16 animate-fade-in">
          <h2 class="text-4xl md:text-5xl font-bold mb-6 inline-block relative">
            Our <span class="text-primary">Certifications</span>
            <span class="absolute bottom-0 left-1/2 w-24 h-1 bg-gradient-primary -translate-x-1/2 rounded-full animate-pulse"></span>
          </h2>
          <p class="text-xl max-w-3xl mx-auto text-gray-300">
            Industry recognitions that validate our commitment to excellence
          </p>
        </div>
        <div class="certifications-grid grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
          {[
            { name: 'ISO 9001:2015', image: '/public/certifications/9001.png' },
            { name: 'ISO 14001:2015', image: '/public/certifications/14001.png' },
            { name: 'ISO 45001:2018', image: '/public/certifications/45001.png' }
          ].map((cert, index) => (
            <div class="certification-item bg-gray-800/50 p-8 rounded-xl flex flex-col items-center justify-center text-center transition-all duration-300 hover:transform hover:-translate-y-2 hover:bg-gray-800/70 hover:shadow-lg animate-fade-in" style={`animation-delay: ${index * 150}ms`}>
              <img 
                src={cert.image} 
                alt={cert.name} 
                class="w-48 h-48 md:w-64 md:h-64 mb-6 object-contain transition-transform duration-300 hover:scale-105" 
                loading="lazy"
              />
              <h3 class="text-xl font-semibold text-primary">{cert.name}</h3>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Process Section -->
    <section class="process-section py-20">
      <div class="container mx-auto px-6">
        <div class="section-header text-center mb-16 animate-fade-in">
          <h2 class="text-4xl md:text-5xl font-bold mb-6 inline-block relative">
            Our <span class="text-primary">Process</span>
            <span class="absolute bottom-0 left-1/2 w-24 h-1 bg-gradient-primary -translate-x-1/2 rounded-full animate-pulse"></span>
          </h2>
          <p class="text-xl max-w-3xl mx-auto text-gray-300">
            A methodical approach to bringing your vision to life
          </p>
        </div>
        <div class="process-timeline relative max-w-4xl mx-auto">
          <div class="timeline-line absolute top-0 left-1/2 -translate-x-1/2 w-0.5 h-full bg-gradient-to-b from-primary to-transparent"></div>
          {[
            { title: 'Initial Consultation', text: 'We begin by understanding your vision, requirements, and budget.', side: 'left' },
            { title: 'Concept Planning', text: 'Our team creates detailed plans and visualizations.', side: 'right' },
            { title: 'Estimating & Planning', text: 'We develop comprehensive budgets and timelines.', side: 'left' },
            { title: 'Construction Phase', text: 'Our skilled teams execute with precision and quality.', side: 'right' },
            { title: 'Quality Control', text: 'Rigorous inspections ensure high standards.', side: 'left' },
            { title: 'Project Completion', text: 'Final walkthrough and complete satisfaction.', side: 'right' }
          ].map((step, index) => (
            <div class={`timeline-item relative w-1/2 p-8 mb-10 ${step.side === 'left' ? 'left-0 text-right pr-16' : 'left-1/2 pl-16'} animate-slide-in-${step.side}`} style={`animation-delay: ${index * 200}ms`}>
              <div class={`timeline-marker absolute top-8 ${step.side === 'left' ? 'right-[-20px]' : 'left-[-20px]'} w-10 h-10 bg-primary rounded-full flex items-center justify-center text-black font-bold z-10 transition-all duration-300 hover:scale-110`}>{index + 1}</div>
              <div class="timeline-content p-8 bg-white/5 rounded-lg shadow-lg hover:shadow-xl transition-all duration-300">
                <h3 class="timeline-title text-2xl font-semibold mb-4 text-primary">{step.title}</h3>
                <p class="timeline-text text-base leading-relaxed text-gray-300">{step.text}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
    
    <!-- CTA Section -->
    <section class="cta-section py-20 text-center">
      <div class="cta-container max-w-3xl mx-auto p-16 bg-white/5 rounded-lg relative overflow-hidden animate-fade-in-up">
        <div class="cta-pattern absolute inset-0 opacity-5" style="background-image: radial-gradient(var(--primary) 2px, transparent 2px); background-size: 20px 20px;"></div>
        <h2 class="cta-title text-4xl md:text-5xl font-bold mb-5 text-white">Ready to Start Your Project?</h2>
        <p class="cta-text text-lg max-w-xl mx-auto mb-8 text-gray-300">
          Contact us today to discuss your construction needs and discover how Ultraction can bring your vision to life.
        </p>
        <a href="/contact" class="cta-button inline-block px-10 py-4 bg-gradient-primary text-white no-underline font-semibold rounded transition-all duration-300 hover:transform hover:-translate-y-1 hover:shadow-lg hover:shadow-primary/30 animate-pulse-slow">Request a Consultation</a>
      </div>
    </section>
  </div>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Services Carousel Functionality
    const serviceTabs = document.querySelectorAll('.service-tab');
    const servicePanels = document.querySelectorAll('.service-panel');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');
    const panelIds = ['buildings', 'bridges', 'water', 'power', 'seaports', 'utilities', 'oilgas', 'heritage', 'pipeline', 'roadworks'];
    let currentIndex = 0;
    
    function showPanel(index, direction = 'next') {
      servicePanels.forEach(panel => {
        panel.classList.remove('active', 'opacity-100');
        if (panel.id === `${panelIds[index]}-panel`) {
          panel.classList.remove(direction === 'next' ? 'translate-x-full' : 'translate-x-[-100%]');
          panel.classList.add('translate-x-0', 'rotate-y-0');
        } else if (panel.id.includes(panelIds[index < panelIds.length - 1 ? index + 1 : 0])) {
          panel.classList.add('translate-x-full', 'rotate-y-15');
          panel.classList.remove('translate-x-0', 'translate-x-[-100%]');
        } else {
          panel.classList.add('translate-x-[-100%]', 'rotate-y--15');
          panel.classList.remove('translate-x-0', 'translate-x-full');
        }
      });
      
      const currentPanel = document.getElementById(`${panelIds[index]}-panel`);
      if (currentPanel) {
        setTimeout(() => {
          currentPanel.classList.add('active', 'opacity-100');
        }, 50);
      }
      
      serviceTabs.forEach(tab => {
        tab.classList.remove('active', 'text-white', 'bg-primary/20');
        tab.classList.add('text-gray-400', 'bg-white/5');
      });
      
      const activeTab = document.querySelector(`.service-tab[data-tab="${panelIds[index]}"]`);
      if (activeTab) {
        activeTab.classList.add('active', 'text-white', 'bg-primary/20');
        activeTab.classList.remove('text-gray-400', 'bg-white/5');
      }
    }
    
    serviceTabs.forEach(tab => {
      tab.addEventListener('click', function() {
        const tabId = this.getAttribute('data-tab');
        const newIndex = panelIds.indexOf(tabId);
        const direction = newIndex > currentIndex ? 'next' : 'prev';
        currentIndex = newIndex;
        showPanel(currentIndex, direction);
      });
    });
    
    prevBtn?.addEventListener('click', function() {
      currentIndex = (currentIndex - 1 + panelIds.length) % panelIds.length;
      showPanel(currentIndex, 'prev');
    });
    
    nextBtn?.addEventListener('click', function() {
      currentIndex = (currentIndex + 1) % panelIds.length;
      showPanel(currentIndex, 'next');
    });
    
    showPanel(currentIndex);

    // Counter Animation for Stats
    const counters = document.querySelectorAll('.stat-number');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const counter = entry.target;
          const target = parseInt(counter.getAttribute('data-count'));
          let count = 0;
          const increment = target / 50;
          
          const updateCounter = () => {
            count += increment;
            if (count >= target) {
              counter.textContent = `${target}${counter.textContent.includes('%') ? '%' : '+'}`;
            } else {
              counter.textContent = `${Math.ceil(count)}${counter.textContent.includes('%') ? '%' : '+'}`;
              requestAnimationFrame(updateCounter);
            }
          };
          
          updateCounter();
          observer.unobserve(counter);
        }
      });
    }, { threshold: 0.5 });

    counters.forEach(counter => observer.observe(counter));
    
    // Enhanced Mobile Timeline
    function adjustTimelineForMobile() {
      const timelineLine = document.querySelector('.timeline-line');
      const timelineItems = document.querySelectorAll('.timeline-item');
      
      if (window.innerWidth <= 992) {
        if (timelineLine) timelineLine.style.left = '20px';
        timelineItems.forEach(item => {
          item.style.width = '100%';
          item.style.paddingLeft = '60px';
          item.style.paddingRight = '30px';
          item.style.textAlign = 'left';
          item.classList.remove('left-0', 'left-1/2');
          const marker = item.querySelector('.timeline-marker');
          if (marker) marker.style.left = '0';
        });
      } else {
        if (timelineLine) timelineLine.style.left = '50%';
        timelineItems.forEach((item, index) => {
          item.style.width = '50%';
          item.style.textAlign = index % 2 === 0 ? 'right' : 'left';
          item.classList.remove('left-0', 'left-1/2');
          item.classList.add(index % 2 === 0 ? 'left-0' : 'left-1/2');
          item.style.padding = '2rem 4rem';
          const marker = item.querySelector('.timeline-marker');
          if (marker) marker.style[index % 2 === 0 ? 'right' : 'left'] = '-20px';
        });
      }
    }
    
    adjustTimelineForMobile();
    window.addEventListener('resize', adjustTimelineForMobile);
  });
</script>

<style>
  :root {
    --primary: #ff6b35;
    --accent: #ff8c66;
    --gradient-primary: linear-gradient(to right, #ff6b35, #ff8c66);
  }

  .perspective-1000 {
    perspective: 1000px;
  }

  .service-tab.active {
    background: var(--primary) !important;
    color: white !important;
    box-shadow: 0 4px 14px rgba(255, 107, 53, 0.3);
  }

  .service-panel {
    transform-style: preserve-3d;
    transition: all 0.7s ease-in-out;
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  .animate-fade-in-down {
    animation: fadeInDown 1s ease-out forwards;
  }

  .animate-slide-in-right {
    animation: slideInRight 0.8s ease-out forwards;
  }

  .animate-bounce-in {
    animation: bounceIn 0.8s ease-out forwards;
  }

  .animate-pulse-slow {
    animation: pulseSlow 2s infinite ease-in-out;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fadeInDown {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes slideInRight {
    from { opacity: 0; transform: translateX(20px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes bounceIn {
    0% { transform: scale(0.8); opacity: 0; }
    60% { transform: scale(1.05); opacity: 1; }
    100% { transform: scale(1); }
  }

  @keyframes pulseSlow {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.02); }
  }

  @media (max-width: 768px) {
    .services-tabs { gap: 2; }
    .service-tab { width: 45%; margin-bottom: 8px; }
    .service-content { padding: 1rem; }
    .service-image { margin-bottom: 1rem; }
    .page-header {
      height: 400px !important; /* Smaller height for mobile */
    }
  }

  @media (max-width: 640px) {
    .page-header {
      height: 350px !important; /* Even smaller for very small screens */
    }
  }
</style>